name: Deploy GitHub Pages

on:
  push:
    branches: [main]
    # Skip Pages when only docs, scripts, or non-site metadata change.
    # Use workflow_dispatch to force a redeploy after those-only merges.
    paths-ignore:
      - "docs/**"
      - "scripts/**"
      - "platform-spec/**"
      - ".hallmark/**"
      - "**/*.md"
      - "LICENSE"
      - "LICENSE.*"
      - ".github/dependabot.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v7
        with: { node-version: 22, cache: npm }
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with: { path: out }
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
